def main(private (field, bool)[7] data) -> field{
    /*
        This zk-proof is to show that the aggregation data is indeed correct
        data: a list of (val, zk_proof)
            val: the value of the daily data of the sensor
            zl_proof: the zk-proof of the value showing it is from the sensor
    */
    field sum = 0;
    for u32 i in 0..7 {
        assert(data[i].1 == true); // verify the zk-proof of the data is correct
        sum = sum + data[i].0;
    }
    return sum;
}